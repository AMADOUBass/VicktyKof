# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/

# Env files (NEVER commit these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Prisma
prisma/migrations/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# TypeScript
*.tsbuildinfo
next-env.d.ts

#claude 
claude/
.claude/
# i wanna ignore the claude folder but not the .claude folder, so i can keep my local claude config in there without it being committed to git
# if i just ignore the claude folder, then the .claude folder will also be ignored, which is not what i want
claude/*
!.claude/
